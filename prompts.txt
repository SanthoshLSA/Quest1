================================================================================
STEP 1: Architecture Finalization & Environment Setup
Date/Time: 2026-08-25 12:52 IST
Prompt Intent: Set Audio Speech-to-Text (Whisper) as the Primary Fast Anchor search engine, with Visual OCR as the fine frame pinpointer and silent-video fallback. Establish repository structure and dependency requirements.
--------------------------------------------------------------------------------
USER PROMPT:
"Let's reverse the process and make it so we find the text through speech first, then if that fails let's proceed with OCR. Proceed with Step 1 environment setup."

TECHNICAL CONTEXT & REASONING:
- Designed a dual-modal search pipeline.
- Audio STT (Whisper) acts as the fast $O(1)$ anchor search, scanning 1 hour of audio in ~30s.
- Visual OCR (EasyOCR) pinpoints the exact starting visual frame around the audio timestamp window or acts as a full coarse-to-fine fallback if no spoken audio match is found.
================================================================================
